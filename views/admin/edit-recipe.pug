extends ../layouts/main-layout.pug

block title 
    title Editing-#{dish}

block styles
    link(rel="stylesheet", href="/styles/main.css")
    link(rel="stylesheet", href="/styles/detail.css")
    link(rel="stylesheet", href="/styles/form.css")

block additional-nav-links 
    li 
        a(href="/admin/add-recipe") Add Recipe 

block content 
    main 
        section.heading 
            h1 #{dish}
            a(href='/recipes?dish='+dish) Cancel 
        section.edit-form 
            form.name-description(action="/admin/edit-recipe", method="POST")
                .step-name
                    label(for="name") Dish Name
                    input#dish-input(type="text", name="name", placeholder=dish)
                .step-description 
                    label(for="description") Description
                    textarea#description-input(name="description", placeholder="Start typing to add a description")
                button(type="submit") Update Recipe